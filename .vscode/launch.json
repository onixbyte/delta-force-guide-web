{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "request": "launch",
      "name": "pnpm dev",
      "command": "pnpm dev",
      "cwd": "${workspaceFolder}"
    },
    {
      "type": "node-terminal",
      "request": "launch",
      "name": "build",
      "command": "pnpm build",
      "cwd": "${workspaceFolder}"
    }
  ]
}
